declare module 'tmai-api' {
  // Minimal ambient declarations to satisfy TypeScript without installing type defs.
  // At runtime, DataProvider dynamically imports and uses the SDK.
  export const TokenMetricsClient: any;
  const _default: any;
  export default _default;
}
